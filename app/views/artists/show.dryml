<show-page>
  <append-content-body:>
      <if test="&@this.lps.count > 0">
        El artista <%= @this.name %> tiene <a href="/lps?artist=#{@this.name}"> <%= @this.lps.count %> LPs</a>
      </if><else>
        El artista no tiene Lps
      </else>
  </append-content-body:>
</show-page>
